# V13 が終わった後の方針（2026-09-26 にユーザーと合意）

```text
status=WAITING_FOR_V13_TO_FINISH
decided_by=ユーザー
```

## 今（V13 が終わるまで）

- 新しい銘柄のデータは取らない。使うのは V13 から除外済みの306銘柄だけ（`EXP-002_HANDOFF.md` のルールを守る）
- 実際のお金は R（EXP-003 の単純なルール）で動かす。注文は `forward/orders/`
- Claude（L）は EXP-004 変更1の方法で、最初の決算シーズン後（2027年1月末ごろ）に判定する
- LightGBM は、306銘柄のデータでの試行を EXP-006 で終えた（EXP-005、EXP-006 とも単純なルールに負けた）。M はペーパーの記録だけ続ける

## V13 が終わった後

1. **方針を変える**：過去のデータは自由に使ってよい（開発用）。本番の判定は、案のルールを決めてコミットした後の「未来のデータ」だけで行う
2. **東証の全銘柄（約3,800）のデータを J-Quants から取る**（約2〜3時間）
3. **LightGBM を全銘柄で作り直す**：過去データで自由に開発し、未来のテストに出す案を1〜2個に絞る
4. **未来のテストに加える**：R（お金）・L（Claude）と並べてペーパーで走らせる。判定は銘柄ごとの成績で行い、早く結論を出す
5. 単純なルール（R）も全銘柄に広げた版を、同じく未来のデータで確かめる

## 理由

- 未来のデータは誰も見ていないので、最も確実な本番テストになる
- 過去のデータを「1回しか使えない」と縛ると、開発が進まない
- ただし V13 は「まだ見ていない過去データで1回だけ試す」設計なので、その本番テストが終わるまでは今のルールを守る
